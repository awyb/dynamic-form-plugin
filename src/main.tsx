import React from 'react'
import ReactDOM from 'react-dom/client'
import { AllTypesDemo } from './demo-all-types'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AllTypesDemo />
  </React.StrictMode>,
)
